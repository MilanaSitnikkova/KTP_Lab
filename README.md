Лабораторная №1
Задание 1. Создайте программу, которая находит и выводит все простые числа меньше 100.

Объяснение работы кода: Запуск главного цикла: В методе main мы создаем цикл, который по очереди перебирает числа от 2 до 99 (строго меньше 100). Каждое число мы отправляем в созданную нами функцию isPrime. Если функция подтверждает, что число простое, мы выводим его в консоль через пробел. Реализация функции проверки: Мы создаем метод isPrime, который принимает число n и проверяет, является ли оно простым. Внутри метода мы запускаем цикл, который ищет делители в диапазоне от 2 до 
n
−
1
. Если исходное число делится на текущий шаг цикла без остатка (n % i == 0), то метод сразу возвращает false, так как число составное. Если цикл завершается и ни одного делителя не найдено, метод возвращает true.

Задание 2. Создайте программу, которая определяет, является ли введенная строка палиндромом.

Объяснение работы кода:

Обработка входных данных: В методе main мы создаем цикл, который поочередно перебирает все строки, переданные в программу через аргументы командной строки args. Каждую строку мы отправляем на проверку в созданный нами метод isPalindrome. В зависимости от ответа программы мы выводим сообщение, является слово палиндромом или нет.
Переворот строки: Мы создаем вспомогательный метод reverseString, который принимает строку и возвращает ее в перевернутом виде. Для этого внутри метода мы запускаем цикл, который перебирает символы исходного слова с самого конца к началу и посимвольно собирает их в новую строку reversed.
Реализация функции проверки: Мы создаем логический метод isPalindrome, который определяет, одинаково ли читается слово в обоих направлениях. Внутри него мы сначала переворачиваем входящую строку с помощью нашего метода reverseString, а затем сравниваем получившийся результат с оригиналом через s.equals(reversed). Если они полностью совпадают, метод возвращает true.


Лабораторная №2

Класс Book (Абстрактный) Поля: title (String), author (String), year (int) — скрытые; totalBooks (static int). Конструкторы: Основной и по умолчанию (заглушки «Неизвестно», 0). Увеличивают totalBooks. Методы: Геттеры, сеттеры, статический getCount(). Функции: Абстрактный display(); getInfo() (возвращает строку с данными; если год 0 — «не указан»).

Класс AudioFormat (Абстрактный, наследует Book) Поля: duration (protected int). Конструкторы: Вызывают super(...), задают duration (в дефолтном — 0). Методы: Геттер, сеттер, переопределенный getInfo() (добавляет длительность к родительской строке).

Класс Audiobook (Наследует AudioFormat) Поля: narrator (private String). Конструкторы: Вызывают super(...), задают narrator (в дефолтном — «Неизвестен»). Методы: Геттер, сеттер. Полиморфизм: Реализует display() («Аудиокнига: [Название] читает [Чтец]») и переопределяет getInfo() (добавляет чтеца). Перегрузка: display(String comment) (выводит display() + комментарий).

Класс MP3Audiobook (Наследует Audiobook) Поля: bitrate (private int). Конструкторы: Вызывают super(...), дефолтный битрейт — 128. Методы: Геттер, сеттер, уникальный playSample() («Воспроизведение фрагмента: [Название]»). Переопределение: display() («MP3 аудиокнига: [Название], битрейт: [Значение] kbps») и getInfo() (добавляет битрейт).

Класс FLACAudiobook (Наследует Audiobook) Поля: quality (private String). Конструкторы: Вызывают super(...), дефолтное качество — «CD Quality». Методы: Геттер, сеттер, уникальный showQuality() («Качество звука: [Значение]»). Переопределение: display() («FLAC аудиокнига: [Название], качество: [Качество]») и getInfo() (добавляет маркер FLAC и качество).

Класс VisualFormat (Абстрактный, наследует Book) Поля: director (protected String). Конструкторы: Вызывают super(...), задают director (в дефолтном — «Неизвестен»). Методы: Геттер, сеттер, переопределенный getInfo() (добавляет режиссера к родительской строке).

Класс Movie (Наследует VisualFormat) Поля: duration (private int). Конструкторы: Вызывают super(...), задают duration (в дефолтном — 0). Методы: Геттер, сеттер, уникальный watchTrailer() («Смотреть трейлер: [Название]»). Полиморфизм: Реализует display() («Фильм: [Название], режиссер: [Режиссер], [Длительность] мин») и переопределяет getInfo() (добавляет длительность).

Класс Musical (Наследует VisualFormat) Поля: composer (private String). Конструкторы: Вызывают super(...), задают composer (в дефолтном — «Неизвестен»). Методы: Геттер, сеттер, уникальный listenAria() («Слушать арию из мюзикла: [Название]»). Полиморфизм: Реализует display() («Мюзил: [Название], режиссер: [Режиссер], музыка: [Композитор]») и переопределяет getInfo() (добавляет композитора).

Лабораторная №8
Метаданные: DataProcessor
Пользовательская аннотация-маркер, предназначенная для разметки целевых методов обработки.

Область видимости (Retention): RUNTIME — метаданные сохраняются в байт-коде и доступны во время работы программы для Reflection API.

Цель применения (Target): METHOD — аннотировать можно только методы.

Параметры: Содержит атрибут description() для логирования и документирования шагов конвейера.

Модель данных (DTO): Item
Сущность, инкапсулирующая данные одной записи (ID, текстовое описание, числовое значение).

Переопределяет метод toString() для сериализации объекта в текстовую строку CSV-формата.

Содержит статический фабричный метод fromString(String line), отвечающий за десериализацию (парсинг) строк, считанных из внешнего источника.

Модули бизнес-логики (Процессоры)
Каждый процессор представляет собой изолированный класс, реализующий конкретный этап обработки данных с использованием декларативного подхода Stream API:

FilterProcessor: Отвечает за условную выборку элементов. Использует оператор .filter(), отсекая записи, значение которых ниже установленного порога (меньше 100.0).

TransformProcessor: Отвечает за модификацию данных. Использует оператор .map() для изменения состояния объектов в потоке (приведение текста к верхнему регистру и коррекция числового значения на +10%).

AggregationProcessor: Отвечает за упорядочивание данных. Использует оператор .sorted() совместно с реверсивным компаратором для сортировки коллекции по убыванию.

Контроллер конвейера: DataManager
Центральное ядро и оркестратор приложения. Выполняет следующие задачи:

Файловый ввод-вывод (I/O): Потоковое чтение (BufferedReader) и запись (BufferedWriter) файлов-источников с обработкой исключений IOException.

Динамическая интроспекция: Через метод registerDataProcessor(Object processor) регистрирует компоненты. В методе processData() с помощью Reflection API сканирует методы зарегистрированных объектов, находит те, что помечены аннотацией DataProcessor, и вызывает их через Method.invoke().

Управление многопоточностью: Инициализирует пул потоков ExecutorService с фиксированным количеством воркеров (Executors.newFixedThreadPool(4)). Каждый этап обработки оборачивается в асинхронную задачу Callable. Синхронизация между этапами и получение обновленного датасета обеспечиваются через блокирующий вызов .get() у объектов Future.

Точка входа: Main
Класс-конфигуратор, который инициализирует жизненный цикл приложения. Он генерирует демонстрационный файл с входными данными, создает экземпляр DataManager, внедряет зависимости (регистрирует процессоры) и запускает последовательное выполнение конвейера.

Алгоритм и жизненный цикл обработки данных

Инициализация: Main создает файл source_data.csv и регистрирует процессоры в DataManager.

Загрузка: DataManager считывает CSV-файл строка за строкой, парсит их через Item.fromString() и формирует исходную коллекцию List в оперативной памяти.

Сканирование и вызов: Контроллер обходит список процессоров. С помощью рефлексии извлекаются методы с аннотацией DataProcessor.

Асинхронное выполнение: Каждая найденная задача отправляется в пул потоков. Поток-воркер выполняет стрим-обработку над текущим состоянием коллекции. Главный поток ожидает завершения текущего этапа через Future.get(), после чего передает результат на вход следующему процессору (конвейерный паттерн).

Фиксация результатов: После прохождения всех этапов DataManager сохраняет финальное состояние коллекции в processed_data.csv и корректно высвобождает ресурсы, вызывая executorService.shutdown().

Технологический стек

Язык разработки: Java (версия 8 и выше)

Парадигмы: Объектно-ориентированное программирование (Инкапсуляция, Модульность), Функциональное программирование (Лямбда-выражения, Stream API).

Метапрограммирование: Java Custom Annotations, Reflection API (java.lang.reflect).

Конкурентность (Concurrency): Java Concurrency Utilities (java.util.concurrent), Thread Pools (ExecutorService), Асинхронные вычисления (Callable, Future).
